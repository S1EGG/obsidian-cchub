- [x] 对 ACP 进行完全重构（先移除后重写，做更好的规划，可以参考/_tmp/aionui 的实现逻辑，同时也要看官方文档来理解他们是怎么实现的https://agentclientprotocol.com/overview/introduction），使其更加灵活的配置和扩展，将ACP 配置与 ACP 的协调功能完全独立开，以便更好的管理，实现类似于将每个 ACP 都做成可插拔模块，根据用户的配置来对应的展示。
- [ ] 对 Settings 进行重新设计，在 Settings 中变更 Active Agent 过程中，不要直接作用到对话框内，而是点击新对话之后才生效，避免产生过多 Bug
- [ ] 对新建对话（+）进行重新设计，点击之后出现下拉菜单，可以选择已经配置好的 CLI，在 CLI 内创建新的对话
- [ ] Settings 界面重新设计，分成多个 tab，tab1主要用来做 general 设置，tab2主要用来针对每个 cli 进行配置工作。
- [ ] 添加记录功能，1）给每个对话根据其第一次对话prompt 生成一个标题，并且将标题适配到聊天框最上方。2）在 export 左侧添加一个聊天记录功能，使用户能够进入到指定的聊天 session 中。（这个功能需要进行一定调研，确认 ACP 协议下的聊天记录存储如何进行）。
- [ ] 错误提示完全优化，当前错误提示有过多 bug，需要进行重构，使其可读性增强。
